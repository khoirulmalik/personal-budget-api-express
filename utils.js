const myEnvelope = require("./myEnvelope");

console.log(myEnvelope[0].id);

// const findById = (arr, id) => {
//   const envelopeExits = (arr, newEnvelope) => {
//     arr.some((envelope) => envelope.id === newEnvelope.id);
//   };

//   if (envelopeExits) {
//     console.log(`id already exits`);
//   } else {
//     arr.push[newEnvelope];
//   }

//   console.log(newEnvelope);
// };

// findById(myEnvelope, 2);
